# تقرير جاهزية الإنتاج — «وقتي» (qa-production-readiness)

```
التطبيق: وقتي (Waqti)          الإصدار: 1.3.3 (build 9)
الفرع: claude/greeting-pwj4sc   الوسم: v1.3.3
التاريخ: 2026-09-09            المُختبِر: Claude (وكيل) — القرار للمالك
البيئة: بناء release بلا خادم (Supabase/Sentry غير مفعّلين)
جهاز الاختبار: لا يوجد بعد (بيئة سحابية) — انظر البوابة 13

النتيجة: BLOCKED     البوابات: 17 | PASS 12 | FAIL 0 | BLOCKED 2 | NOT RUN 3
```

| # | البوابة | الحالة | الدليل / السبب |
|---|---|---|---|
| 1 | البناء | PASS | Release Builds (release.yml) — v1.3.2 run #15 ناجح بنفس الإعداد؛ v1.3.3 يُشغَّل مع هذا الالتزام (يُحدَّث الرقم في `memory/TEST_RESULTS.md`) |
| 2 | التحليل الساكن | PASS | `flutter analyze --fatal-infos` = 0 مشكلات (محليًا + Quality Gate) |
| 3 | اختبارات الوحدة | PASS | 103 نجحت / 0 فشلت / 1 متخطّى (`flutter test`, 2026-09-09) |
| 4 | اختبارات الواجهة | PASS | widget flows: onboarding→home، إنجاز مهمة، التقويم، تبديل اللغة، a11y، حارس المحرر، الامتثال، الحجب عن بُعد |
| 5 | اختبارات التكامل (الرحلة الحرجة) | PASS | `flows_test.dart` + مسح متصفح حقيقي (Playwright) على بناء الويب: أول تشغيل → زائر → إضافة/إنجاز → الإعدادات، 0 أخطاء |
| 6 | اختبارات API | PASS (غير منطبق مُثبَت) | لا API مخصص ولا خادم في هذا البناء (`AppConfig.hasSupabase=false`). عند التفعيل: `test/integration/rls_test.dart` إلزامي |
| 7 | الفحص الأمني | PASS | تاريخ git: 0 أنماط أسرار؛ 0 ملفات `.env/.jks/.p8` متتبَّعة؛ R8+obfuscate؛ لا cleartext؛ جلسة في Keystore/Keychain؛ فحص CI (`quality.yml`) |
| 8 | فحص الأداء | NOT RUN | يحتاج جهازًا حقيقيًا في وضع release (`flutter run --profile`) — يُنفَّذ في المرحلة 3 |
| 9 | فحص قاعدة البيانات | PASS (غير منطبق مُثبَت) | لا قاعدة في هذا البناء. للتفعيل: `supabase/tests/rls_checks.sql` + `rls_test.dart` |
| 10 | اختبار الشبكة | PASS (نطاق مخفَّف موثَّق) | الاستخدام الأساسي بلا شبكة؛ الجلب الوحيد (`app-config.json`) يفشل مفتوحًا — `remote_config_test.dart` |
| 11 | إعدادات الإنتاج | PASS | `quality.yml`: لا `http://`/`localhost` في lib، لا `debuggable`، لا cleartext؛ الروابط العامة `AppLinks.*` |
| 12 | الأسرار | PASS | لا BLOCKED مسموح هنا — الفحص أعلاه (7) + `.gitignore` + secrets scan في CI |
| 13 | حزمة الإصدار على جهاز حقيقي | NOT RUN | لا جهاز في البيئة. المطلوب: تثبيت `waqti-v1.3.3.apk` على أندرويد حقيقي + TestFlight على iPhone، رحلة كاملة + تذكير والتطبيق مغلق (R8) |
| 14 | تقارير الأعطال | BLOCKED | `SENTRY_DSN` لدى المالك فقط؛ الشيفرة جاهزة (release/dist/environment، بلا PII). يُرفع فور إضافة السر — قبل الرفع للمتاجر |
| 15 | النسخ الاحتياطي | PASS | لا بيانات خادم في هذا البناء؛ النسخة المحلية اليومية + الاستعادة مُختبرة (`app_state_test`: daily local backup can be restored) |
| 16 | مفتاح إيقاف عن بُعد | PASS (وحدة/واجهة) · NOT RUN (تفعيل حقيقي) | `web/app-config.json` + `RemoteConfig`؛ اختبار widget يُظهر شاشة الحجب ويزيلها. تفعيل حقيقي بتعديل الملف على Pages يُنفَّذ مع اختبار الجهاز |
| 17 | الخصوصية ونموذج سلامة البيانات | BLOCKED | السياسة منشورة وتعمل (privacy/support/delete-account)؛ النماذج تُملأ في لوحتي المتجر بعد إنشاء الحسابين — الإجابات جاهزة في `docs/SUBMISSION_PACK.md` §5 |

**الأعطال المانعة:** لا شيء مفتوح (`memory/BUGS.md`).

**BLOCKED — السبب والأثر وتاريخ الرفع:**
- 14: سر خارجي يملكه المالك؛ الأثر: لا رؤية للأعطال بعد النشر؛ يُرفع فور إضافة `SENTRY_DSN` (قبل أول رفع للمتجر).
- 17: حسابا المتجر غير منشأين؛ الأثر: لا رفع أصلًا قبل ذلك؛ يُرفع في المرحلة 1–2 من `docs/LAUNCH_PHASES.md`.

**القرار:** NO-GO للرفع إلى الإنتاج حتى: (أ) البوابة 13 PASS على جهاز أندرويد حقيقي + iPhone، (ب) البوابة 8 مقاسة، (ج) رفع BLOCKED 14 و17.
GO لرفع **Internal testing / TestFlight** فور توفر الحسابات (البوابات 1–7، 11، 12 كافية لمسار الاختبار).

مالك القرار: داود — التاريخ: ______ (يوقَّع بعد اختبار الجهاز)
