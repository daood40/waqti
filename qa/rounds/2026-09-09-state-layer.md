# جولة كسر — طبقة الحالة والنماذج (2026-09-09)

الميزة تحت الاختبار: `AppState` + `models.dart` (إضافة/تعديل/استيراد/تصدير/تصنيفات/حدود/تواريخ/حساب).
معايير القبول: كل مدخل خاطئ يُرفض أو يُطبَّع بلا انهيار؛ الحدود المعلنة في الواجهة (اسم ≤ 60، تذكيرات ≤ 3، هدف 1–100000) تُفرض في الطبقة لا في الواجهة فقط.
النطاق: الطبقة المنطقية (بلا واجهة). خارج النطاق: الجهاز الحقيقي، الشبكة.
الأداة: `test/adversarial_test.dart` (كل اختبار = محاولة كسر).

| # | المحاولة | المدخل/الفعل | المتوقّع | الفعلي | الحكم | رقم العطل |
|---|---|---|---|---|---|---|
| 1 | اسم مهمة فارغ/مسافات | `addTask('   ')` | رفض | قُبل | فشل | BUG-001 |
| 2 | اسم 5000 حرف | `addTask` | قصّ إلى 60 | حُفظ 5000 | فشل | BUG-002 |
| 3 | رموز خاصة `<script>` | `addTask` | يُحفظ كنص | يُحفظ كنص | نجح | — |
| 4 | قيمة تبدأ بـ `=` في CSV | `exportCsv` | تُحيَّد (حقن صيغة Excel) | تُصدَّر كما هي | فشل | BUG-003 |
| 5 | تصنيف فارغ أو مكرر | `addCategory` | رفض/إرجاع الموجود | يُضاف | فشل | BUG-004 |
| 6 | نفس المعرّف مرتين | `addTask` | رفض | يُضاف | فشل | BUG-005 |
| 7 | هدف 0 / سالب | ctor + JSON | غير قابل للقياس / ≥1 | كما هو متوقع | نجح | — |
| 8 | تقدم فوق الهدف / تحت الصفر | `incrementProgress` ×20، −100 | يُقيَّد | يُقيَّد | نجح | — |
| 9 | 6 تذكيرات في JSON | `fromJson` | تُقصّ إلى 3 | 6 | فشل | BUG-006 |
| 10 | فهارس خارج النطاق | reorder/restore/toggle/cycle | لا انهيار | لا انهيار | نجح | — |
| 11 | ساعات هدوء عابرة/متساوية | `isQuietAt` | صحيح | صحيح | نجح | — |
| 12 | 29 فبراير 2028 | `isApplicableOn` | صالح | صالح | نجح | — |
| 13 | 23:59 / 00:00 | `DateKey` | يومان مختلفان | كذلك | نجح | — |
| 14 | ديسمبر → يناير | `monthStats` | لا تسرّب | لا تسرّب | نجح | — |
| 15 | «مرة واحدة» بلا تاريخ | `isApplicableOn` | لا ينطبق | لا ينطبق | نجح | — |
| 16 | JSON تالف (6 صيغ) | `importJson` | رفض بلا تغيير | رفض | نجح | — |
| 17 | مهمة بلا `id` / أنواع خاطئة | `importJson` | تسامح (معرّف مولَّد) | فشل الاستيراد كله | فشل | BUG-007 |
| 18 | ملاحظة بسطر جديد وفواصل | `exportCsv` | اقتباس صحيح | صحيح | نجح | — |
| 19 | فشل حذف الحساب (شبكة) | `deleteAccount` | لا مسح محلي | لا مسح | نجح | — |

الملخّص: 19 محاولة · 7 أعطال (0 حرج، 1 مهم: BUG-003 حقن صيغة CSV، 6 بسيطة: تحقق في الطبقة). لم يُغطَّ: الجهاز الحقيقي، الشبكة (خارج النطاق).
الحكم: **غير جاهزة حتى إصلاح السبعة** → أُصلحت في نفس اليوم (انظر memory/FIXES.md) وأُعيد تشغيل الجولة: 19/19 نجح.
