<div dir="rtl">

# PROJECT_STATUS — حالة مشروع وقتي

> آخر تحديث: 2026-08-29 · المراجع الملزمة:
> [`docs/WAQTI_TRANSFORMATION_DIRECTIVE_v3.md`](docs/WAQTI_TRANSFORMATION_DIRECTIVE_v3.md) (الأحدث)
> + [`docs/WAQTI_MASTER_DIRECTIVE_v2.md`](docs/WAQTI_MASTER_DIRECTIVE_v2.md)

## مستجدات 2026-08-29 (جلسة التحويل الاحترافي — بدايتها)

- اعتُمد توجيه التحويل v3 وأُنجزت **Phase 0 بالكامل**: الفحوص الـ18 في
  [`docs/AUDIT.md`](docs/AUDIT.md) وخارطة الطريق بمراحل v3 السبع عشرة في
  [`docs/ROADMAP.md`](docs/ROADMAP.md).
- **Phase 4 (Dashboard Today-first) — الدفعة الأولى منفذة ومختبرة:**
  - ترويسة اليوم: تحية بحسب الوقت + التاريخ الكامل + ساعة حيّة.
  - بطاقة "مهام اليوم" أصبحت أول الشاشة، مفروزة **بالأولوية**
    (عاجلة ← عالية ← متوسطة ← منخفضة) مع تمييز لوني للمعلّق العاجل/العالي.
  - أولوية رابعة **عاجلة (Urgent)** عبر النموذج والمحرر والشارات.
  - بطاقة **"مهام متأخرة"** (آخر 7 أيام، مقيدة بـ `createdAt` الجديد) —
    نقرة واحدة تُنجز اليوم الفائت.
  - بانر الترقية نُقل أسفل محتوى اليوم (اليوم أولًا وفق v3).
  - توحيد البحث (الاسم + الوصف) في الرئيسية.
  - صلابة: تلف مخزون `shared_preferences` لم يعد قادرًا على تبييض الشاشة.
  - 31 اختبارًا خضراء + تحقق متصفحي موثق (عربي/إنجليزي، فاتح/داكن).
- التالي: استكمال Phase 2 (رموز التصميم) ثم Phase 5 (Drift + نموذج
  المهام الكامل: تواريخ استحقاق، مهام فرعية، وسوم، Quick Add متعدد).

## أين نحن الآن

- **التطبيق الحالي**: تطبيق Flutter عامل ومختبَر (تحويل كامل للنموذج الأولي `design/waqti_prototype.html`) يعمل على Android وiOS والويب، عربي/إنجليزي مع RTL كامل، فاتح/داكن/تلقائي، وحفظ محلي عبر `shared_preferences`.
- **الموقع المنشور**: https://daood40.github.io/waqti/ (ينشر تلقائياً مع كل push).
- **الجودة**: `dart format` نظيف، `flutter analyze` بلا ملاحظات، 27 اختباراً تنجح، وبناء الويب والـ APK يعملان في CI.
- **المرحلة وفق التوجيه v2**: ما قبل المرحلة 0 — أُنجز منها هذه الجلسة: الوثائق (ADRs، ARCHITECTURE، ROADMAP، SYNC، هذا الملف). المتبقي من المرحلة 0: Drift Schema وDesign Tokens الموسعة وإعادة الهيكلة Feature-based.

## تحليل الفجوات (Gap Analysis) مقابل التوجيه v2

### ✅ موجود يُبقى (مطابق للتوجيه أو أساس صالح)

| البند في التوجيه | الوضع الحالي |
|---|---|
| ADR-001: البناء الحالي أساس؛ React مؤرشفة | `archive/react-version` موجود؛ Flutter في الجذر |
| المرجع البصري = النموذج الأولي (§6) | مطابقة بصرية مُتحقق منها بلقطات شاشة (الألوان/الخطوط/الشاشات) |
| RTL أولاً + LTR، فاتح/داكن/System (§6) | مكتمل ويعمل |
| المهام: إنشاء/تعديل/حذف مع Undo (§9، ملحق هـ) | مكتمل (حذف مع تراجع Snackbar) |
| الأولويات والتصنيفات (§9) | موجودة (priority + categories) |
| إعادة الترتيب بالسحب `sort_order` (§9) | موجودة في تبويب المهام |
| العادات: Streak حالي وأطول + Heatmap سنوي (§13) | موجودة (شبكة شهرية + خريطة سنة حرارية) |
| الإحصائيات تُحسب محلياً (ADR-007، §21) | كذلك هي اليوم |
| الإنجازات تُحافَظ عليها (§22) | موجودة (مستويات + XP + شارات) |
| الاشتراكات Free/Premium كما في النموذج (§32) | واجهة كاملة (محاكاة بلا دفع فعلي) |
| الاستخدام بلا حساب مسموح (§29) | "المتابعة كزائر" موجودة |
| تصدير/استيراد البيانات JSON (§30) | موجود في الإعدادات |
| CI/CD: Flutter APK + Web على Pages — يُحافَظ عليه (§36) | `.github/workflows/` تعمل مع كل push |

### 🔧 موجود يُعدَّل (أساس قائم يحتاج ترقية للمواصفة)

| البند | الحالي | المطلوب في v2 |
|---|---|---|
| التخزين | `shared_preferences` (JSON واحد) | SQLite/Drift بمخطط الجداول (§27) — ADR-002 |
| إدارة الحالة | Provider (ChangeNotifier مركزي) | Riverpod + Clean Architecture Feature-based (ADR-003) |
| التكرار | 5 أنواع مخصصة (يومي/أسبوعي/شهري/أيام محددة/مرة) | RRULE (RFC 5545) + استثناءات + توليد كسول (§10، ADR-004) |
| حالات المهمة | pending/done/late/missed لكل يوم | فصل `status` عن سجل الإكمال والعروض الذكية (§9) |
| التنقل | 6 تبويبات + زر + | 5 تبويبات + "المزيد" (§7) |
| الإنجازات | قواعد في الكود | قواعد في `achievements.json` (§22) |
| البحث | فلترة أسماء المهام | بحث شامل FTS5 عبر الكيانات (§23) |
| الاشتراك | أسعار في الكود (محاكاة) | خطط من Backend `plans` بلا أسعار في الكود (§32) |
| إشعارات | تفضيلات فقط (بلا جدولة فعلية) | إشعارات محلية فعلية مُعاد جدولتها بعد إعادة التشغيل (§18) |

### 🆕 جديد (غير موجود اليوم — حسب مراحل التنفيذ §39)

| المرحلة | البنود |
|---|---|
| 0 | Drift Schema كامل، Design Tokens موسعة، هيكلة `features/` |
| 1 | Smart Views (Inbox/Today/Upcoming)، مهام فرعية، وسوم، مرفقات/ملاحظات، إشعارات محلية، بحث FTS5 |
| 2 | Focus/Pomodoro موثوق، تتبّع الوقت `time_entries`، عادات كمية، قواعد Skip للـ Streak |
| 3 | تقويم Day/Week/Month، المخطط اليومي `time_blocks`، الأهداف الهرمية، المشاريع، المراجعات اليومية/الأسبوعية، مواقيت الصلاة (اختياري)، هجري (عرض) |
| 4 | Supabase: مصادقة حقيقية، محرك مزامنة (`sync_queue`، LWW حقلي، Tombstones)، اشتراكات فعلية، لوحة إدارة |
| 5 | مساعد AI خلف Flag، استيراد تقويمات، مشاريع جماعية |

## قرارات هذه الجلسة

- اعتماد التوجيه v2 وإدراجه في `docs/WAQTI_MASTER_DIRECTIVE_v2.md`.
- توثيق ADR-001 حتى ADR-007 في `docs/adr/` كما وردت في §25.
- إنشاء `docs/ARCHITECTURE.md` (الحالي + الهدف)، `docs/ROADMAP.md` (المراحل والموقع الحالي)، `docs/SYNC.md` (تصميم محرك المزامنة §28).
- عدم المساس بـ `archive/react-version` ولا بسير عمل CI — كلاهما محفوظ.
- الانتقال إلى Drift/Riverpod **لن يُنفَّذ دفعة واحدة**؛ يُنفَّذ في المرحلة 0→1 بأصغر تعديلات متتابعة تُبقي التطبيق عاملاً بعد كل خطوة (قاعدة §46 في التوجيه: أصغر تعديل يحقق المتطلب).

## المطلوب من المالك (Credentials — ملحق ج)

عند الوصول للمرحلة 4: مشروع Supabase (URL + Anon Key)، Google/Apple Sign-In، حسابات المتاجر، مزود الدفع، Sentry DSN. لا شيء مطلوب للمراحل 0–3.

</div>

## جلسة تجهيز الإطلاق (2026-09-02)

- توقيع أندرويد عبر `android/key.properties` (خارج المستودع) مع رجوع آمن لمفاتيح debug.
- منصة ويندوز أُنشئت (`windows/`) مع أيقونة `.ico` من الشعار و`msix_config` لمتجر مايكروسوفت.
- iOS: بند `ITSAppUsesNonExemptEncryption=false` أُضيف؛ الاسم والأيقونات جاهزة.
- سير عمل جديد `release.yml` (يدوي/وسم v*) ينتج حِزم: AAB+APK، ويندوز zip+MSIX، ويب zip، iOS بلا توقيع.
- الدليل الكامل: `docs/RELEASE.md`.
- اختبار شامل في متصفح حقيقي: دخول زائر، إضافة مهمة وإكمالها (1/1)، التبويبات الستة، الإنجليزية + الداكن. كل البوابات خضراء.

## جلسة إغلاق فجوات السوق (2026-09-02، مساءً)

- بحث 100 تطبيق: `docs/MARKET_RESEARCH_100.md` + `docs/COMPETITIVE_ANALYSIS.md`.
- نُفّذ: درجة عادة مرنة، أطول سلسلة، مهام فرعية، يوميات، فترات اليوم، 3 تذكيرات، عادات إقلاع، ساعات هدوء، نسخة احتياطية محلية + استعادة، CSV، جولة تعريفية، تصفية/فرز، حكمة اليوم، اهتزاز، عادات كمية، يوم راحة، إيقاف مؤقت، مؤقت تركيز، هجري، قوالب، إشعارات محلية.
- وضع الإطلاق (`kLaunchMode`): كل المزايا مجانية، الباقات «قريبًا» — لا تدفق شراء (توافق مراجعة المتاجر).
- الإصدار v1.1.0 يُنشر كـ GitHub Release بروابط مباشرة عبر `release.yml` (مدخل version).
- 48 اختبارًا؛ مراجعة أمنية `docs/SECURITY_REVIEW.md`؛ دليل الإطلاق `docs/LAUNCH_PLAYBOOK.md`.
- الإصدار v1.2.0 (1.2.0+3) يحمل وضع الإطلاق؛ MSIX للمتجر يُبنى عند وجود أسرار `MSIX_PUBLISHER`/`MSIX_IDENTITY_NAME`/`MSIX_PUBLISHER_DISPLAY_NAME`، وإلا حزمة اختبارية.
- معاينة وضع الإطلاق في متصفح حقيقي: عربي فاتح + إنجليزي داكن — الشارة و«قريبًا» صحيحتان.

## جلسة تدقيق الأزرار (2026-09-03)

- مسح آلي بالنقر على كل عناصر التطبيق في متصفح حقيقي (Playwright + شجرة الوصولية): لا أخطاء من التطبيق؛ الأخطاء الوحيدة من محرك Flutter في وضع الوصولية فقط.
- إصلاحات v1.2.1: التصدير يُنزّل ملفًا على الويب (`lib/core/export_file*.dart`) مع نسخ إلى الحافظة وبديل نافذة عند فشله؛ زر «قريبًا» في الباقات يعرض تنبيه فترة الإطلاق؛ إخفاء دخول Google/Apple الوهمي في وضع الإطلاق؛ أيام التقويم تفتح ورقة اليوم مع تبديل الحالة (لا أيام مستقبلية).

